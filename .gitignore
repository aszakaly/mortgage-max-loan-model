# Regenerated by the pipeline (rebuild with the scripts — see README)
mortgage_clean.csv
model_final.joblib
model_full.joblib
predictions_test.csv

# Export artifacts (regenerated from the .pptx during QA)
*.pdf

# Duplicate of the raw CSV
archive (1).zip

# OS / tooling
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
